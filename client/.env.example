# In development this file is NOT needed — the Vite proxy forwards /api to localhost:5000.
# In production (Vercel), set VITE_API_URL to your Railway backend URL.
# Example:
VITE_API_URL=https://communicare-api.up.railway.app
